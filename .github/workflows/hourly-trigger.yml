name: hourly-trigger
on:
  schedule:
    - cron: '0 * * * *'   # top of every hour UTC
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Checkout private ta-bot-ml repo
        uses: actions/checkout@v4
        with:
          repository: cixthefirst-glitch/ta-bot-ml
          token: ${{ secrets.TARGET_REPO_PAT }}
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install -r requirements.txt
      - name: Run scan + broadcast
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ADMIN_CHAT_ID: ${{ secrets.ADMIN_CHAT_ID }}
          MEXC_SECRET_KEY: ${{ secrets.MEXC_SECRET_KEY }}
          MEXC_ACCESS_KEY: ${{ secrets.MEXC_ACCESS_KEY }}
          COINGECKO_API_KEY: ${{ secrets.COINGECKO_API_KEY }}
        run: python bot.py
      - name: Commit updated data back to private repo
        run: |
          git config user.name "ta-bot-ml-trigger"
          git config user.email "bot@ta-ml.local"
          git add data/ || true
          git diff --cached --quiet || git commit -m "auto: hourly scan $(date -u +%FT%TZ)"
          git push
